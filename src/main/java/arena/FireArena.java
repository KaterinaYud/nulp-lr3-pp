package arena;

import java.util.List;

public class FireArena extends Arena {
    public FireArena(int size, List<String> startPoints, int arenaType) {
        super(size, startPoints, arenaType);
    }
}