package arena;

import java.util.List;

public class WaterArena extends Arena {
    public WaterArena(int size, List<String> startPoints, int arenaType) {
        super(size, startPoints, arenaType);
    }
}